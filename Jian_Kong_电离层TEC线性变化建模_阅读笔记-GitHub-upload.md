# 稀疏线性台站的电离层 TEC 建模

> **Kong, Jian; Chu, Ruitao; Peng, Wenjie; Yao, Yibin; Zhang, Qi; Gao, Xin**  
> *Ionospheric TEC modeling approach based on the characteristics of linear ionospheric variation*  
> **GPS Solutions**, 2025, 29:61 · DOI: [10.1007/s10291-025-01822-y](https://doi.org/10.1007/s10291-025-01822-y)

---

## 1. 研究背景

### 1.1 现有技术的局限性

| 需求 | 现有困难 |
|---|---|
| 高精度 GNSS / PPP 需要实时、精细的电离层改正 | 全球模型（如 IRI、GIM）只能刻画大尺度背景，难以满足局地高精度定位 |
| 区域模型可以更精确 | 往往需要密集的二维参考站网，建设和运维成本高 |
| 铁路、输电线、海岸线等区域是狭长走廊 | 在窄区域内布设二维网的几何条件差；沿线大规模加密台站不经济 |

### 1.2 本文的研究方向

台站沿某个经纬方向排成一条稀疏链仍然准确插值得到目标点的 VTEC并改进 PPP。太阳辐射、地磁场和地球自转共同塑造电离层的经向/纬向结构。因此，在局地范围内，电离层 TEC 沿某些方向具有可被低阶到中阶多项式近似的连续变化。

---

## 2. 论文的完整技术路线

```text
多站双频 GPS 原始观测
        ↓
周跳与粗差处理；载波相位平滑伪距（CCL）
        ↓
计算 STEC，改正卫星与接收机 DCB
        ↓
采用改进单层模型（MSLM）映射为 VTEC，并确定各站 IPP
        ↓
按“同一卫星 + 同一历元”组织多站观测
        ↓
将 IPP 沿台站链方向的距离归一化
        ↓
IRI-2016 加权的自适应阶数多项式拟合（POLY）
        ↓
非负约束，输出目标 IPP 的 VTEC
        ↓
与两站 IDW、近距离三站局部面插值比较
        ↓
将 VTEC 及其精度作为 PPP 中电离层延迟的先验约束
```

### 2.1 参与拟合的数据

不同卫星在同一时刻的视线不同，穿过电离层的位置（IPP）也不同。本文只把**同一颗卫星、同一时刻**由多个地面站观测到的 VTEC 放进同一个模型，因此每一次拟合都对应一条真实的、同步的空间剖面，而不是混合不同卫星和不同时刻的数据。即：多个地面站在特定历元下对同一颗卫星观测。

### 2.2 对比模型

| 方法 | 输入站点 | 空间假设 | 作用 |
|---|---|---|---|
| **POLY** | 沿链的多个台站 | 沿线 TEC 可以由自适应阶数多项式表示 | 本文提出的方法 |
| **TS-IDW** | 目标站两侧的相邻两个台站 | TEC 由距离加权的两点插值给出 | 检验“多站拟合”是否优于两站插值 |
| **SURF** | P127 周围半径 30 km 的 3 个台站 | 近邻局部面插值 | 检验是否仅靠更近、更密的局部站点就足够 |

---

## 3. 数据与实验设计

| 项目 | 论文设置 |
|---|---|
| GNSS 数据 | GPS 双频观测；中纬度数据来自美国西部 UNAVCO，低纬度数据来自澳大利亚 Geoscience Australia |
| 中纬度站网 | 9 个台站分别沿地理经度（GLon）、地磁经度（MLon）、地磁纬度（MLat）排列；相邻约 100 km |
| 低纬度站网 | 沿 GLon 排列；相邻约 200–300 km |
| 对比年份 | 2014 年：高太阳活动，平均 F10.7 为 145.9 SFU；2019 年：低太阳活动，平均 F10.7 为 69.6 SFU |
| 数据质量控制 | 连续观测弧段 > 30 min；卫星高度角 > 15°；进行周跳、粗差和 DCB 处理 |
| 验证方式 | 对线性站链实施交叉验证，考察站链方向、端点/中部位置、季节、太阳活动、磁暴和平静期 |

### PPP 实验设置中的策略

PPP 使用 15 s 采样、截止高度角 15°、卡尔曼滤波每小时重置。收敛标准为：水平与垂直误差均小于 0.1 m，且持续 5 min。

传统 PPP（TRAD）只估计电离层延迟，没有加入任何外部电离层约束；模型增强 PPP 则将 POLY / TS-IDW / SURF 的结果作为电离层延迟的**约束**。

---

## 4. 核心公式

### 4.1 相位平滑伪距提取斜向 TEC（STEC）

```math
\mathrm{STEC}=
\frac{f_1^2 f_2^2}{40.28\left(f_1^2-f_2^2\right)}
\left[L_4-\left\langle P_4+L_4\right\rangle_{\mathrm{arc}}
+c\left(B_r+B^S\right)\right].
```


| 符号 | 含义 |
|---|---|
| $`f_1, f_2`$ | GPS L1、L2 频率，分别为 1575.42 MHz 与 1227.60 MHz |
| $`P_4, L_4`$ | 几何无关的伪距组合与载波相位组合 |
| $`\langle P_4+L_4\rangle_{\mathrm{arc}}`$ | 在一段连续观测弧内的平均量，用于载波相位平滑伪距 |
| $`B_r, B^S`$ | 接收机与卫星的差分码偏差（DCB） |

载波相位提供高精度，伪距帮助消除相位模糊度的常数项。DCB 不改正会被误认为电离层效应，因此必须扣除。

### 4.2 投影函数

```math
\mathrm{STEC}=\mathrm{VTEC}\cdot
\left(1-\frac{R^2}{(R+h)^2}
\sin^2\!\left[\alpha\left(\frac{\pi}{2}-El\right)\right]\right)^{-\frac{1}{2}}.
```


| 符号 | 含义 |
|---|---|
| $`R`$ | 地球半径 |
| $`h`$ | 等效电离层单层高度；文中取 $`506.7\ \mathrm{km}`$ |
| $`El`$ | 卫星高度角 |
| $`\alpha`$ | 改进单层模型参数；文中取 $`0.9782`$ |

高度角越低，映射因子越大，所以低高度角观测对模型误差更敏感，文中采用 $`15^\circ`$ 截止角。

### 4.3 IPP 沿链距离归一化

```math
\tilde d_i=\frac{d_i-\mu}{\sigma},
\qquad
\mu=\frac{1}{n}\sum_{i=1}^{n}d_i,
\qquad
\sigma=\sqrt{\frac{1}{n-1}\sum_{i=1}^{n}(d_i-\mu)^2}.
```


其中，$`d_i`$ 是第 $`i`$ 个 IPP 相对首个 IPP、沿台站链方向的距离；$`\tilde d_i`$ 是归一化距离。

将距离平移到均值为 0、尺度约为 1 的坐标。这样高阶项不会因为距离单位过大而数值不稳定；将一个狭长区域的二维空间问题压缩为沿线路方向的一维函数问题。

### 4.4 POLY 的观测方程与加权最小二乘解

```math
\mathbf L=\mathbf H\mathbf X+\boldsymbol\varepsilon,
\qquad
\hat{\mathbf X}=
\left(\mathbf H^{\mathsf T}\mathbf W\mathbf H\right)^{-1}
\mathbf H^{\mathsf T}\mathbf W\mathbf L.
```


```math
\mathbf H_i=
\left[1,\ \tilde d_i,\ \tilde d_i^2,\ \ldots,\ \tilde d_i^m\right],
\qquad
\mathbf L_i=I_i^S(t).
```


| 符号 | 含义 |
|---|---|
| $`\mathbf L`$ | 同一卫星 $`S`$、同一历元 $`t`$ 下，各 IPP 的 VTEC 观测向量 |
| $`\mathbf H`$ | 由归一化距离及其幂次组成的设计矩阵 |
| $`\mathbf X`$ | 待估多项式系数 |
| $`m`$ | 多项式阶数 |
| $`\mathbf W`$ | 观测权阵 |

模型参数每个历元更新一次，并针对每颗卫星分别计算，权重由IRI-2016给出。得到参数后对TEC进行计算时加入非负约束。

### 4.5 IRI-2016 参与定权

```math
w_i(t)=
\left|
\frac{I_{i,\mathrm{GNSS}}^S(t)-I_{i,\mathrm{IRI\text{-}2016}}^S(t)}
{I_{i,\mathrm{IRI\text{-}2016}}^S(t)}
\right|^{-1}.
```


以第$`i`$个IPP处GNSS观测和IRI-2016输出之间的相对误差作为权重依据。偏差越大权重越小，偏差越小权重越大。

### 4.6 自适应选择多项式阶数

```math
m^{\ast}(t)
=
\operatorname*{arg\,min}_{0 \leq m \leq 7}
\left|
\sum_{k=0}^{m} p_k(t)\,\tilde d_i^{\,m-k}(t)
- I_i^S(t-1)
\right|
```


多项式阶数的选择在每个历元单独确定，最终阶数由上一历元TEC误差决定。

具体操作：用当前历元数据拟合0~7阶的模型，将这7个模型分别应用到上一历元的数据中，选取残差最小的。

### 4.7 两站 IDW、精度指标与 PPP 约束

两站 IDW 的对照式为：

```math
I_i^S(t)=
\frac{I_{i-1}^S(t)W_{i-1}(t)+I_{i+1}^S(t)W_{i+1}(t)}
{W_{i-1}(t)+W_{i+1}(t)},
\qquad
W_k(t)=\frac{1}{\left|\tilde d_i-\tilde d_k\right|}.
```


若 $`e_i=\mathrm{VTEC}_{\mathrm{GPS},i}-\mathrm{VTEC}_{\mathrm{model},i}`$，则

```math
\mathrm{Bias}=\frac{1}{n}\sum_{i=1}^{n}e_i,
\qquad
\mathrm{STD}=\sqrt{\frac{1}{n}\sum_{i=1}^{n}\left(e_i-\mathrm{Bias}\right)^2},
\qquad
\mathrm{RMSE}=\sqrt{\frac{1}{n}\sum_{i=1}^{n}e_i^2}.
```


PPP 中，模型给出的电离层约束标准差设为

```math
\sigma_I=\frac{A}{\sin(El)}\ \mathrm{m}/\sqrt{\mathrm h},
```


其中 $`A`$ 是实验获得的模型精度。

Bias 看系统偏差，STD 看随机离散，RMSE 同时包含二者；PPP 公式则把 TEC 模型误差转换成滤波器中的权重。模型越可信（$`A`$ 越小）、卫星高度角越高，约束越强。精度计算时，采取交叉验证的方式，留一个站不参与建模，用于精度评估。

---

## 5. 实验结果

### 5.1 中纬度高太阳活动年：地理经度方向

| 比较对象 | POLY 的表现 | 结论 |
|---|---:|---|
| 中纬度、2014、沿链交叉验证 | 平均 RMSE **0.32 TECU** | 在约 100 km 站距下已具有较高插值精度 |
| 相邻两站 TS-IDW | 平均精度提升 **39.6%** | 优势来自多站刻画空间曲线，而非简单距离加权 |
| 30 km 内三站 SURF | 平均精度提升 **55.6%** | 更近的站并不自动带来更优结果；站网方向与模型结构更关键 |
| POLY 的系统偏差 | Bias 约 **0.07 TECU** | 系统误差很小 |

在 LT08:00 和 LT18:00 的误差序列中，POLY 的误差分布更集中；SURF 在 LT18:00 出现约 0.49 TECU 的系统偏差。**在狭长区域，合理的线性多站构型可以优于密集的局部面构型。**

### 5.2 台站方向与位置会改变结果

- 沿 **地理经度（GLon）** 与 **地磁经度（MLon）** 的站链，总体优于沿地磁纬度（MLat）的站链。
- 站链中部站通常优于端点站：中部是插值，两端更接近外推，天然更难。
- IPP 坐标误差不能忽略（将IPP从原始位置分别沿经度和纬度认为引入坐标误差）：纬向偏差不超过 10 km 时，RMSE 从 0.35 增至 0.37 TECU；偏差不超过 25 km 时增至 0.47 TECU，精度下降 34.2%。

论文的有效性不只依赖“线性台站，更依赖这条线是否大致沿 TEC 的局地连续变化方向。IPP 的纬向误差更敏感，也说明单层高度和映射函数会影响拟合坐标本身。

### 5.3 不同太阳活动、纬度和站距

| 条件 | 结果 | 解读 |
|---|---:|---|
| 2019 年中纬度低太阳活动 | MLon / GLon 平均 RMSE 为 **0.23 / 0.10 TECU** | 模型在平静太阳活动条件下仍稳定，且 GLon、MLon 优于 MLat |
| 2019 年某异常站 | 最大 RMSE 为 **0.928 TECU** | 可能受磁暴或等离子体泡影响；局地强扰动仍是难点 |
| 低纬度、站距约 200–300 km | 九站总体 RMSE 为 **0.86 TECU** | 站距变大后精度明显下降。 |

### 5.4 PPP 的最终收益

| 方案 | 磁暴期收敛时间 | 平静期收敛时间 | 磁暴期 3D 精度 | 平静期 3D 精度 |
|---|---:|---:|---:|---:|
| **POLY 约束 PPP** | **10.3 min** | **8.8 min** | **0.43 m** | **0.42 m** |
| TS-IDW 约束 PPP | 17.0 min | 13.5 min | 0.65 m | 0.56 m |
| SURF 约束 PPP | 13.8 min | 11.6 min | 0.53 m | 0.53 m |
| 传统 PPP（TRAD） | 22.1 min | 23.2 min | 0.85 m | 0.70 m |

- 相比传统 PPP，POLY 在磁暴期将收敛时间缩短 **52.3%**，在平静期缩短 **61.9%**。
- POLY 在四种方案中均给出最短收敛时间和最佳 3D 精度。
- 说明 POLY 不只是让 TEC 曲线的 RMSE 更小，而是能在 PPP 滤波中提供有价值的先验信息。

---

## 6. 创新点

1. **面向狭长区域重新设计观测构型**：将依赖密集二维网的区域建模问题，转化为稀疏一维台站链上的拟合问题。
2. **用多站曲线拟合取代两站插值**：让模型利用多站的整体空间形态。
3. **逐卫星、逐历元、自适应阶数建模**：参数随时空几何变化，避免对整天或整个区域使用固定函数。
4. **将 IRI 作为随机模型信息**：用背景模型与观测的相对差异调节权重。

## 

## 7.我的想法

IRI-2016定权存在一定隐患，是否可以用上一历元模型结果和当前历元的观测误差定权？

一维假设是否过于强了，能否进行二位狭窄条带建模？拟合不是一个曲线而是一个二维曲面？
