# 时间窗口选择

---

## 1. 文献的研究内容

| 文献 | 对时间窗口问题的核心贡献 | 与本研究最直接的关系 |
|---|---|---|
| Erol & Arıkan (2005) | 以滑动窗一阶、二阶统计量的残差百分比误差，找“局部宽平稳（local WSS）”的最长时间尺度 | 是你“平均 VTEC 是否仍具代表性”“波动强度是否稳定”两项指标的直接方法来源 |
| Nohutcu et al. (2010) | 固定空间模型复杂度，比较 15、30、60、120 min 建模时段，发现时段越长，静态 2-D 模型残差 RMSE 越大；但短窗会减少观测量 | 给出“时间非平稳误差—样本量/数值稳定性”权衡的直接实例，且特别贴近区域 B 样条/梯度拟合 |
| Wang et al. (2024) | 在实时区域模型中采用 20 min 历史滑窗、每 10 min 更新；用内部一致性和外部 dSTEC 精度检验模型 | 是“在时间窗之后，如何做独立模型精度验证”的工程范例；其 20 min 是该数据、模型和实时任务下的设计值，不应直接当作通用最优值 |

**本研究的逻辑**：

- 一阶统计量残差百分比误差：衡量窗口内平均 VTEC是否仍有代表性
- 二阶统计量残差百分比误差：衡量窗口内波动强度是否稳定
- 独立站点预测 RMSE / MAE：衡量窗口对真实模型精度的影响（需要拟合模型计算，可以放在讨论里面？不然上来就拟合了？我感觉可以放在讨论部分）
- IPP 位移统计：衡量时间窗引入的空间平滑尺度（IPP 位移 P50/P95 与 0.25° 分辨尺度的比值）

---

## 2. 统一符号与窗口定义

设同一分析单元中的 VTEC 时间序列为 $X_t$（单位 TECU），采样间隔为 $\Delta t$，候选窗口长度为 $W=N_w\Delta t$。对中心历元 $t_0$，窗口内样本集合写作 $\mathcal W(t_0,W)$。

若直接把不同卫星、不同 IPP、不同测站的 VTEC 混成一条时间序列，统计量会同时反映真实时间变化、空间梯度与卫星几何变化，难以解释为纯粹的时间窗口效应。

- **单站–单卫星连续弧段**：最接近 Erol & Arıkan 的时间序列思路，适合诊断局地时间平稳性；

---

## 3. 指标一：一阶统计量残差百分比误差

Erol & Arıkan 对 VTEC 序列 $X_r$ 使用矩形滑动窗均值：

$$
\hat\mu(n;N_w)=\frac{1}{N_w}\sum_{i=-(N_w-1)/2}^{(N_w-1)/2}X_r(n+i).
$$

其一阶统计量残差百分比误差定义为：

$$
e_f(N_w)=\frac{\lVert X_r-\hat\mu\rVert}{\lVert X_r\rVert}\times100\%.
$$

（Erol & Arıkan, 2005, pp. 376–377，式 (2)、(8)）

它衡量的是：当你用一个长度为 $W$ 的局部平均表示该段 VTEC 时，原始序列相对该局部平均的偏离有多大。随着窗口变长，均值会更平滑，却更难跟随真实的时间变化，故该残差通常随 $W$ 增大。

**分析策略：**

1. 表格，时间窗口，样本数，一节残差百分比误差，相对于前一窗口的增量
2. 绘图，横轴为时间窗口长度，纵轴为一节残差百分比误差

论证：随窗口由 1 min 增加到 10 min，一阶残差百分比误差逐渐增加；当窗口继续扩大到 20 和 30 min 时，误差增长趋势进入平台，或单位时间的误差增幅明显变小。因此，10 min 位于误差曲线的转折/平台起点附近：它已经提供足够样本量来稳定估计窗口均值，同时尚未引入过强的时间平均化偏差，故选择 10 min 作为合理窗口长度。

**平台肘部：**将“肘部”定义为首次满足相邻增量足够小的 $W$，例如：

$$
\frac{e_f(W_{k+1})-e_f(W_k)}{W_{k+1}-W_k}<\tau_f.
$$

阈值 $\tau_f$ 必须在方案中预先写明，或以曲线拟合的拐点算法确定；不要看完结果再临时选择阈值。

---

## 4. 指标二：二阶统计量残差百分比误差

Erol & Arıkan 以 $X_r^2$ 的滑动均值表示二阶原点矩：

$$
\hat\mu_p(n;N_w)=\frac{1}{N_w}\sum_iX_r^2(n+i),
$$

并定义：

$$
e_s(N_w)=\frac{\lVert X_r^2-\hat\mu_p\rVert}{\lVert X_r^2\rVert}\times100\%.
$$

若窗口内 VTEC 的总体幅度和波动结构都比较稳定，$X^2(t)$ 与其窗口平均 $\hat\mu_{p,W}(t)$ 很接近，$e_s(W)$ 小；

若窗口内出现明显变化，例如快速上升/下降、突变或强扰动，$X^2(t)$ 会偏离窗口平均，$e_s(W)$ 变大；

当窗口变长时，平滑更强、越来越难跟踪真实变化，$e_s(W)$ 通常增加。

一阶量稳定只说明平均 TEC 没有明显漂移；若短时扰动、闪烁或突变使波动强度改变，$X^2$ 的局部行为会先失稳。故 $e_s$ 是对“窗口内波动强度可否视为稳定”的补充约束。本指标本质上是 $E[X^2]$ 同时含均值平方与方差。

除复现原文的 $e_s$ 外，建议并列报告滑动样本方差或残差方差：

$$
s^2(t_0;W)=\frac{1}{N_w-1}\sum_{t\in\mathcal W}(X_t-\bar X)^2.
$$

---

## 5. 指标三：独立站点预测 RMSE / MAE

对一个候选窗口 $W$，用训练站（或训练观测）拟合线性梯度模型，再在未参与拟合的独立站点/独立弧段上预测 VTEC。令独立观测为 $y_i$、预测为 $\hat y_i$，则：

$$
\mathrm{RMSE}(W)=\sqrt{\frac{1}{n}\sum_{i=1}^n(y_i-\hat y_i)^2},\qquad
\mathrm{MAE}(W)=\frac{1}{n}\sum_{i=1}^n|y_i-\hat y_i|.
$$

RMSE 对少量大误差更敏感；MAE 更稳健，二者应同时报告，单位均为 TECU。

**这个我觉得可以放在讨论部分**

---

## 6. 指标四：IPP 位移统计与空间平滑尺度

对某条连续卫星–接收机弧段，在中心时刻 $t_0$ 的 IPP 位置记为 $\mathbf p(t_0)$。对窗口内每个观测时刻 $t$，计算球面距离：

$$
d_{IPP}(t;t_0)=R_E\arccos\left[\sin\phi_t\sin\phi_0+\cos\phi_t\cos\phi_0\cos(\lambda_t-\lambda_0)\right].
$$

对于一个窗口，把所有有效弧段、所有 $d_{IPP}$ 汇总，计算：

$$
D_{50}(W)=Q_{0.50}(d_{IPP}),\qquad D_{95}(W)=Q_{0.95}(d_{IPP}).
$$

也可计算端点位移 $d_{end}=d_{IPP}(t_0-W/2,t_0+W/2)$，它更直观但只利用两个端点,它表示这条观测弧在整个窗口中“走过”的距离。

$D_{50}$：50% 的观测距离窗口中心 IPP 不超过多少 km；

$D_{95}$：95% 的观测距离窗口中心 IPP 不超过多少 km。

**统计的时候最好按照仰角分层一下**

**0.25° 不是一个固定的公里尺度：**

- 南北向：约 $0.25\times111.2\approx27.8$ km；
- 东西向：约 $27.8\cos\phi$ km，随纬度 $\phi$ 变化。

如果模型使用 0.25° × 0.25° 经纬网格，可定义当地等效尺度：

$$
L_{cell}(\phi)=\sqrt{(27.8)^2+(27.8\cos\phi)^2}\ \mathrm{km}.
$$

并报告比例 $R_{50}=D_{50}/L_{cell}$、$R_{95}=D_{95}/L_{cell}$。若你的线性梯度模型的实际支持域/节点间距不是 0.25°，应优先用**模型实际空间分辨尺度**替代 $L_{cell}$。

例如：$R_{95}=0.25$，95% 观测相对窗口中心仅移动不超过约四分之一网格尺度，空间混合较弱。

---

## 7. 三篇论文的概述

### 7.1 Erol & Arıkan (2005)

#### 研究问题与方法

作者的根本问题是：电离层全天显著时变，不能对整天数据假定平稳；那么在多长的局部滑动时间窗内，VTEC 的一、二阶统计量仍可以可靠估计？

- 4 个站：Kiruna（高纬）、Kiev、Ankara、M. Dragot（中纬）；
- 2001 年太阳活动极大期的一周，含负扰动日、平静日、正扰动日；
- VTEC 以 30 s 分辨率估计，仰角截止 $10^\circ$；
- 使用矩形滑窗，比较不同 $N_w$ 下的 $e_f$、$e_s$，找 local WSS 时段。

### 7.2 Nohutcu, Karslioglu & Schmidt (2010)

#### 研究问题与方法

该文比较两类区域 VTEC 表示：

- 2-D：Sun-fixed 经度 + 地理纬度，假设建模时段内电离层近似静态；
- 3-D：地理经度 + 纬度 + 时间，显式表示时变。

使用 27 个土耳其双频 GPS 站、30 s 数据、$15^\circ$ 仰角截止、400 km 单层高度；以二次 B 样条、IRLS 稳健回归和 LSQR 求解。（pp. 617–622）

作者固定 2-D B 样条层级，围绕相同中点（15:30 UT）比较 15、30、60、120 min 时段。RMSE 随窗口增大而增加：

| 建模时段 | 15 min | 30 min | 60 min | 120 min |
|---|---:|---:|---:|---:|
| 2-D B 样条残差 RMSE (TECU) | 1.0886 | 1.1168 | 1.1433 | 1.1708 |

这组数字非常适合写进文献综述，但措辞应准确：它表明**该案例中，固定空间复杂度的静态 2-D 模型对更长时段的平均化误差更大**；它不是“任何数据窗口越短越好”的证明。

### 7.3 Wang et al. (2024)

区域实时星间单差电离层模型采用二阶多项式。作者设：

- 观测历史窗口：**20 min**；
- 数据采样率：**30 s**；
- 滑窗步长/系数更新间隔：**10 min**；
- 参考时刻：窗口中点；
- 使用过去 20 min 的观测估计 9 个二阶多项式系数。

#### 精度检验框架

- **内部一致性**：模型量与同网络 UU-PPP 提取的星间单差斜向延迟比较；
- **外部一致性**：用 GF 载波相位历元差分导出的 dSTEC 变化作为参考，与模型 dSTEC 变化比较；
- 外部验证的计算间隔设为 5 min，剔除仰角不超过 $10^\circ$ 的结果；
- 测试使用建模网络之外的 4 个站（文中红站），并跨连续 10 天汇总 RMS。

---

## 8. 可以形成的论文表述

“时间窗口长度并非仅由观测数量决定。过短窗口虽能降低电离层非平稳性引起的平均化偏差，却会削弱空间几何覆盖并增大梯度参数的不确定性；过长窗口则将真实时间变化混入静态空间模型。本文首先依据滑动窗口内 VTEC 一阶与二阶统计量残差百分比误差诊断局部宽平稳时间尺度，随后在满足 IPP 空间位移与参数可识别性约束的候选窗口中，通过留站交叉验证的 VTEC 预测 RMSE 和 MAE 确定窗口长度。IPP 位移分位数用于量化窗口引入的时空混叠尺度。”

这段的引用建议：一、二阶统计量方法引 Erol & Arıkan (2005)；时间窗与静态模型误差/数据量权衡引 Nohutcu et al. (2010)；独立外部验证和实时滑窗工程结构引 Wang et al. (2024)；IPP 位移指标标注为“本文提出的几何诊断”。

---

## 参考文献

- Erol, C. B., & Arıkan, F. (2005). Statistical Characterization of the Ionosphere Using GPS Signals. *Journal of Electromagnetic Waves and Applications, 19*(3), 373–387. https://doi.org/10.1163/1569393054139615
- Nohutcu, M., Karslioglu, M. O., & Schmidt, M. (2010). B-spline Modeling of VTEC over Turkey Using GPS Observations. *Journal of Atmospheric and Solar-Terrestrial Physics, 72*(7–8), 617–624. https://doi.org/10.1016/j.jastp.2010.02.022
- Wang, A., Zhang, Y., Chen, J., Liu, X., & Wang, H. (2024). Regional Real-Time Between-Satellite Single-Differenced Ionospheric Model Establishing by Multi-GNSS Single-Frequency Observations: Performance Evaluation and PPP Augmentation. *Remote Sensing, 16*(9), 1511. https://doi.org/10.3390/rs16091511
